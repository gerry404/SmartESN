spring:
  application:
    name: backend
  datasource:
    url: jdbc:postgresql://localhost:5432/smartesn
    username: postgres
    password: genius
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true